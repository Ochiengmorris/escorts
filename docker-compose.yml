services:
  db:
    image: postgres:16
    restart: always
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=escorts
    ports:
      - 5430:5432
    volumes:
      - db-data:/var/lib/postgresql/data

volumes:
  db-data:
